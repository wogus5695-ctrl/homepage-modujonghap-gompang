# 배포 전 SEO & 마크업 무결성 검수 체크리스트 (SEO Checklist)

본 문서는 키워드 추가, 템플릿 수정, 허브 확장 등의 코드 변경 작업 후 네이버 SEO 및 검색엔진 수집 규격이 무너지지 않도록 배포 전에 반드시 거쳐야 하는 **재발 방지용 표준 가이드**입니다.

---

## 1. 템플릿별 핵심 점검 기준

### [공통 사항]
- [ ] **robots.txt**: `/_next/` 경로 크롤링 차단 상태 유지 확인 (`src/app/robots.ts`에 `disallow: ['/private/', '/_next/']` 정상 등록 여부)
- [ ] **메타 이미지 경로**: `og:image`, `twitter:image`, JSON-LD 및 HTML 소스 안의 대표 이미지 주소가 `/_next/image`가 아닌 **public의 물리 파일 주소** (`https://www.modujonghap.co.kr/thumbnail.jpg` 등)를 가리키는지 확인
- [ ] **내부 직링크**: 서비스 내부의 모든 링크는 리다이렉션을 거치지 않는 **200 OK 최종 주소** (`/k/...`)로 직접 연결되어 있는지 확인

---

### [메인 페이지 (/) / 곰팡이 랜딩 템플릿]
- [ ] **H1 단일화**: 화면에 표출되는 H1 태그가 모바일/PC 통합하여 **DOM 내부에 단 1개**만 존재하는지 확인 (`src/components/Hero.tsx` 내 Unified H1 구조 유지)
- [ ] **서비스군 매칭**: 곰팡이군 페이지에 `탄성코트`나 `줄눈시공` 관련 문구가 노출되거나 footer, Hero pill badge 등에 해당 단어가 섞여 들어가지 않았는지 확인
- [ ] **Canonical & og:url**: `?k=` 형태가 아닌 **`/k/지역명-작업명` 형태의 고유 URL**이 canonical로 기재되어 있는지 확인

---

### [마감/인테리어 랜딩 템플릿 (finish군)]
- [ ] **H1 단일화**: 곰팡이군과 마찬가지로 Hero 영역 내 `<h1>`이 단 1개만 존재하는지 확인
- [ ] **문구 독립성**: `곰팡이제거`, `단열시공`, `결로방지` 등의 단어가 footer, FAQ 도입부, CTA 버튼, 본문 등에 절대 섞이지 않았는지 확인 (탄성코트, 줄눈시공 중심 어휘만 노출)
- [ ] **CTA 매칭**: 하단 최종 문의 및 Hero 버튼이 `사진 상담 & 마감 견적 문의`를 가리키는지 확인

---

### [허브 페이지 (/sitemap-seoul, /sitemap-seoul-finish)]
- [ ] **H1 단일화**: 허브 페이지 최상단 타이틀에만 H1이 적용되어 있는지 확인
- [ ] **H2 구조화**: 각 자치구/시 지역명 헤더는 H3가 아닌 **H2**로 통일성 있게 매칭되었는지 확인
- [ ] **지역별 소개 단락**: 단순 키워드 링크 모음으로 인한 저품질 필터링 방지를 위해 지역 아래에 **1줄 이상의 고유 소개 문장**이 실시간 출력되는지 확인
- [ ] **하단 전용 CTA**: 허브 페이지 가장 하단에 서비스군 성격에 어울리는 **CTA 카드**가 렌더링되어 작동하는지 확인
- [ ] **DOM 직접 노출**: 검색 봇이 앵커 텍스트를 즉시 긁어갈 수 있도록 접기/펼치기 동작 여부와 상관없이 모든 링크 태그가 HTML 소스코드 상에 상시 존재해야 함

---

## 2. 사이트맵 세그멘테이션 점검
- [ ] **사이트맵 인덱스**: `/sitemap.xml` 주소 요청 시 하위 분할 사이트맵 리스트가 올바르게 반환되는지 확인
- [ ] **정적 파일 사이트맵 (`/sitemap-main.xml`)**: 메인과 허브 페이지 3개만 들어있는지 확인
- [ ] **곰팡이군 사이트맵 (`/sitemap-mold.xml`)**: 탄성코트/줄눈을 제외한 곰팡이/단열/결로 관련 200 URL만 누적되어 있는지 확인
- [ ] **마감군 사이트맵 (`/sitemap-finish.xml`)**: 탄성코트/줄눈 관련 200 URL만 누적되어 있는지 확인
- [ ] **리다이렉트 제외**: 구형 쿼리파라미터 주소(`?k=...`)가 사이트맵에 단 하나도 섞이지 않았는지 확인

---

## 3. 신규 키워드 추가 시 필수 준수 절차
신규 자치구, 동 또는 서비스 키워드를 추가할 때는 다음 파일들에 순차적으로 키워드를 연동해야 합니다.

1. **데이터 스키마 등록**: `src/lib/dynamicHome.ts` 내의 `areaData` 배열에 해당 동/시/구와 하위 행정동 추가
2. **동적 문구 확인**: `dynamicHome.ts`의 서비스군 분기문에 새 서비스가 추가되었을 경우 알맞은 문구 템플릿(H1, Subtitle, IntroText, BottomEndingText, FAQ 1/2)이 작성되었는지 확인
3. **허브 페이지 동기화**: `sitemap-seoul/page.tsx` 또는 `sitemap-seoul-finish/page.tsx` 내부의 `areaData` 및 `services` 배열에 키워드를 동일하게 입력하여 허브 목록에 즉시 노출되는지 확인
4. **사이트맵 동기화**: `src/app/sitemap.ts` 내부의 `areaData` 배열에도 추가한 키워드를 동일하게 매칭하여 신규 URL이 sitemap.xml 빌드 시 자동 포함되는지 확인

---

## 4. 검수용 샘플 URL 테스트 매뉴얼
배포를 올린 직후 아래 샘플 URL들을 직접 방문하거나 브라우저 개발자 도구(F12)의 `Elements` 탭 및 소스 보기(`Ctrl+U`)를 통해 최종 검수합니다.

| 분류 | 검수 대상 테스트 URL | 예상 상태 코드 및 검수 포인트 |
| :--- | :--- | :--- |
| **메인** | `https://www.modujonghap.co.kr/` | 200 OK, H1 개수 1개 확인 |
| **리다이렉트** | `https://www.modujonghap.co.kr/?k=강서구-곰팡이제거` | **301 Redirect** -> `/k/강서구-곰팡이제거`로 자동 전환 여부 |
| **곰팡이 상세** | `https://www.modujonghap.co.kr/k/강서구-곰팡이제거` | 200 OK, H1 개수 1개, canonical `/k/강서구-곰팡이제거`, 곰팡이 전용 문구 확인 |
| **단열 상세** | `https://www.modujonghap.co.kr/k/화곡동-단열시공` | 200 OK, 단열 전용 FAQ 및 첫 문단 렌더링 확인 |
| **탄성 상세** | `https://www.modujonghap.co.kr/k/강서구-탄성코트` | 200 OK, finish 전용 문구(오염방지/리모델링) 노출 확인, 곰팡이 어휘 미노출 확인 |
| **줄눈 상세** | `https://www.modujonghap.co.kr/k/가양동-줄눈시공` | 200 OK, finish 전용 FAQ 및 `사진 상담 & 마감 견적 문의` CTA 매칭 확인 |
| **곰팡이 허브** | `https://www.modujonghap.co.kr/sitemap-seoul` | H1 1개, 자치구 H2 확인, 각 자치구 아래 소개글 존재 확인 |
| **마감 허브** | `https://www.modujonghap.co.kr/sitemap-seoul-finish` | H1 1개, 자치구 H2 확인, 하단 마감 전용 CTA 카드 작동 확인 |
| **사이트맵** | `https://www.modujonghap.co.kr/sitemap.xml` | 3종의 서브 sitemap 리스트 정상 표출 여부 확인 |

---

## 5. 배포 후 사이트맵 (Sitemap) 최종 무결성 검수 10대 체크리스트

운영 서버에 배포가 완료된 직후, 브라우저 주소창이나 curl 명령어를 통해 반드시 확인해야 할 10가지 정합성 기준입니다.

- [ ] **1. sitemap.xml 인덱스 출력 확인**: 브라우저에서 `/sitemap.xml`을 열었을 때, `<sitemapindex>` 태그 규격 하위에 `sitemap-main.xml`, `sitemap-mold.xml`, `sitemap-finish.xml` 3개의 하위 경로가 정상 표출되는지 확인
- [ ] **2. 3대 서브 사이트맵 200 OK 응답 확인**: 각 개별 사이트맵 주소(main, mold, finish) 호출 시 404가 발생하지 않고 HTTP 상태 코드 200 성공 응답을 반환하는지 확인
- [ ] **3. sitemap-main.xml 내 고정 페이지 격리 확인**: `/sitemap-main.xml` 안에 오직 메인 루트(`/`), 곰팡이 허브(`/sitemap-seoul`), 마감 허브(`/sitemap-seoul-finish`) 3가지의 정적 주소만 들어있고 동적 키워드(`/k/...`) 주소는 완전히 제외되었는지 확인
- [ ] **4. sitemap-mold.xml 내 곰팡이군 URL 노출 확인**: `/sitemap-mold.xml` 내에 `곰팡이제거`, `단열시공`, `결로방지` 서비스에 대한 상세 `/k/지역명-작업명` URL 목록이 `<url><loc>` 태그로 정상 노출되는지 확인
- [ ] **5. sitemap-finish.xml 내 finish군 URL 노출 확인**: `/sitemap-finish.xml` 내에 `탄성코트`, `줄눈시공` 서비스에 대한 상세 `/k/지역명-작업명` URL 목록이 `<url><loc>` 태그로 정상 노출되는지 확인
- [ ] **6. 구형 ?k= 쿼리 파라미터 제외**: 모든 XML 소스 내에 `?k=` 형태의 주소가 완전히 제외되고 깨끗한 `/k/...` 형식만 존재하는지 확인
- [ ] **7. 내부 처리 리소스 URL 제외**: `/_next/image?url=...` 등 콘텐츠와 상관없는 Next.js 시스템 URL이 단 하나도 섞이지 않았는지 확인
- [ ] **8. 중복 URL 필터링**: 관악구/강남구 신사동 등 동일한 동명 키워드로 인해 중복 생성된 상세 주소가 제거되고 오직 순수 고유 URL셋만 누적되어 있는지 확인
- [ ] **9. 브라우저 XML 파싱 에러 유무 확인**: 브라우저(Chrome/Safari/Edge 등)로 사이트맵 URL을 직접 호출했을 때, `XML parsing error` 문구 없이 정상적인 트랙 구조(또는 XML 문서 스타일)로 깨끗하게 열리는지 확인
- [ ] **10. 신규 키워드 자동 반영 모델 확인**: 새 행정구역 정보 추가 시 별도의 사이트맵 파일 코딩 없이 중앙 데이터(`src/lib/areaData.ts`) 추가만으로 사이트맵 파일 빌드 시 자동 연동 및 포함되는지 검증

---

## 6. H1/헤딩 마크업 무결성 점검 체크리스트 (Heading SEO Integrity)

배포 직후 또는 컴포넌트 구조 변경 시 다음 목록을 기준으로 H1 및 헤딩 구조의 무결성을 수동/자동 검수합니다.

### [검수 대상 범위]
1. **메인 페이지** (`/`)
2. **곰팡이군 상세 페이지** (샘플 최소 10개 이상 확인)
3. **finish군 상세 페이지** (샘플 최소 10개 이상 확인)
4. **허브 페이지 2종** (`/sitemap-seoul`, `/sitemap-seoul-finish`)

### [체크리스트 세부 항목]
- [ ] **1. 페이지당 물리적 H1 개수 단 1개 여부**: 브라우저 검사기(Elements) 기준 해당 HTML 페이지 내에 `<h1>` 태그가 정확히 **1개**만 생성되어 배치되어 있는지 검수
- [ ] **2. 원시 소스코드(view-source) 기준 H1 무결성**: 브라우저 소스 보기(`Ctrl+U`) 상태에서 `<h1` 키워드로 검색했을 때 일치하는 검색 결과가 오직 **1개**만 검색되는지 확인 (SSR 출력 단계부터 H1 단일성 보장)
- [ ] **3. DOM 내 숨겨진 중복 헤딩(duplicate heading) 존재 여부**: 모바일용/PC용 분기를 위해 화면상에 표시되지 않지만 HTML 소스코드에 남겨진 중복 `h1`, `h2` 마크업 요소가 제거되었는지 확인 (CSS `hidden` 처리된 heading 검출 방지)
- [ ] **4. 보조 문구의 일반 텍스트(Non-Heading) 처리**: 메인 타이틀(H1) 하위의 보조 슬로건, 리드 서브 카피, 설명 단락은 절대 헤딩 태그(`h1`, `h2`, `h3`, `h4`)로 감싸지 않고 `<p>` 또는 `<div>` 내 일반 텍스트로 처리했는지 확인
- [ ] **5. PC/모바일용 이중 렌더링 배제**: 동일 문구를 다른 클래스로 2번 출력하는 중복 코드를 제거하고, 개행(`br`)과 CSS 유틸리티만으로 단일 문장에 반응형 스케일을 입혔는지 확인
- [ ] **6. Hero 영역 공통 구조 규격 준수**: 모든 템플릿의 Hero 영역은 아래의 규격을 엄격히 지키는지 점검
  *   `Label/Badge` (span 또는 div 사용)
  *   `Title` (오직 **h1** 1개만 사용)
  *   `Lead/Subtitle` (p 또는 div 사용)
  *   `Body/IntroText` (p 또는 div 사용)
- [ ] **7. 템플릿/서비스군 불문 공통 기준 준수**: 메인, 곰팡이군 상세, finish군 상세, 허브 페이지 모두 위의 1~6 규격이 예외 없이 일관되게 작용되고 있는지 교차 검수

